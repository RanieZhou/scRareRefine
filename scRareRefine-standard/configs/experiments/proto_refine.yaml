experiment_name: proto_refine
dataset: TODO
seed: 42
method: scRareRefine_proto
base_outputs: TODO
prototype:
  distance_metric: cosine
  use_train_only: true
fusion:
  alpha: TODO
uncertainty_gate:
  enabled: false
rare_adjustment:
  enabled: false
notes: "Template only. Parameters must not be tuned on test labels."
