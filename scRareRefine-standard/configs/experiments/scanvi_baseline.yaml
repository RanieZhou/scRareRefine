experiment_name: scanvi_baseline
dataset: TODO
seed: 42
method: scANVI
outputs:
  save_latent: true
  save_probabilities: true
  save_predictions: true
notes: "Template only. Create dataset/seed-specific configs before running formal experiments."
